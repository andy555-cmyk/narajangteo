# 나라장터 과업 서칭 (공고 수집 → RFP 추출 → 제안서 골격)

이승원 대표(Andy)의 프로젝트다. 존댓말. 팩트 기반, 추정 금지(불확실은 '확인 필요'). 환각 금지. 리스크는 직설적으로 지적한다.

## 세션 시작 시 먼저 읽을 것

본부(HQ) = `/Users/iseung-won/Library/CloudStorage/GoogleDrive-ganpan0@gmail.com/내 드라이브/hayday-hq`

| 파일 | 내용 |
|---|---|
| `03_시스템_도구/나라장터_과업서칭/00_개요.md` | 한 줄 정의, 원자료 위치 |
| `03_시스템_도구/나라장터_과업서칭/01_고정지식.md` | API·파라미터·필터·키워드 사전·공고 스펙 (32KB) |
| `03_시스템_도구/나라장터_과업서칭/02_의사결정.md` | 결정과 WHY |
| `03_시스템_도구/나라장터_과업서칭/04_미결.md` | 막힌 것, 대표 결정 대기 |
| `.claude/나라장터_과업서칭_SOP.md` | 반복 실행 절차 |

## 이 저장소

- 원격: `github.com/andy555-cmyk/narajangteo` (main → GitHub Pages 자동 배포). **remote URL의 PAT는 제거 완료(2026-07-28 확인).**
- 대시보드: `https://andy555-cmyk.github.io/narajangteo/`
- 코드: `g2b_scan.py`(수집) / `g2b_spec.py` / `g2b_rfp.py`(첨부 파싱) / `build_dashboard.py` / `build_report.py` / `build_bundle.py`
- 산출: `index.html`(= `g2b_dashboard.html`, 빌드가 둘 다 씀), `reports.html`, `report_R26BK*.html`, `reports_data/*.json`, `.nojekyll`
- 일일 갱신: 클라우드 스케줄 트리거가 매일 10시 KST에 `narajangteo-bot`으로 커밋. **이 맥의 전원과 무관하다.** GitHub Actions·로컬 cron 아님.

## 실측 상태 (2026-07-28) — 인수인계 문서 숫자는 7/21 스냅샷이라 다르다

- 채택 **28건**(필터 보정 전 39건 → 노이즈 11건 제거) / 자동추출 25 / 부울경 11
- 판정 분포 **적극 1 · 조건부 1 · 보류 6 · 미분석 20** — 인수인계 기재(보류21·적극4·조건부3)와 다르다.
  판정이 나빠진 게 아니라 **리포트가 수동이라 일일 스캔을 못 따라가서** 미분석이 쌓인 것이다(미결 H-22).
- ⚠ 위 숫자는 **7/28 로컬 재빌드 기준**이다. 원격 `index.html`은 아직 개선 전 산출물이며,
  **2026-07-29 10:00 KST 봇 실행 때 새 코드로 재생성되면서 자동 정정된다**(아래 참조).

## 배포 상태 (2026-07-28) — 소스는 반영, 산출물은 봇 대기

- ✅ `g2b_scan.py`(`162b6a5`) · `build_dashboard.py`(`cf459a1`) → **origin/main 반영 완료**
  (맥에 git 인증이 없어 GitHub 웹 에디터로 커밋했다. 원격 파일과 로컬 파일 SHA-256 일치 확인.)
- ⏳ `index.html`·`g2b_dashboard.html`·`g2b_result.csv`·`attachments.json`·`specs.json`·`store.json`
  → **푸시하지 않았다.** 봇이 매일 재생성하는 산출물이라 다음 실행에서 새 코드로 덮어써진다.
  원격 코드로 7/29 시나리오를 시뮬레이션해 정상 산출(D-day 롤포워드·임박 7건)을 확인했다.
- ⚠ 따라서 **7/29 10:00 이전까지 라이브 페이지는 옛 화면**이다. 그 시각 이후 자동 정상화된다.
  그때 `https://andy555-cmyk.github.io/narajangteo/`에서 '마감 D-7 이내'가 0이 아닌지 꼭 확인할 것.

## 이 저장소에서 조심할 것 (실측으로 확인된 함정)

1. **키워드 단일 진실 원천은 `g2b_scan.py`다. `keywords.json`이 아니다.**
   인수인계 SOP-2는 반대로 적고 있다. `keywords.json`은 스캔이 write만 하는 파생 출력물이라
   그것만 고치면 필터는 안 바뀌고 다음 스캔에 덮어써진다. (미결 H-21)
2. **D-day 기준일을 다시 하드코딩하지 말 것.** `build_dashboard.py`의 `BASE_DATE`는 `GEN_AT` 우선 →
   `today()` 폴백이다. 과거 `date(2026,7,18)` 고정이라 마감임박 표시가 열흘간 죽어 있었다. (H-20, 해소)
3. **로컬에서 `g2b_scan.py`는 못 돌린다** — `SERVICE_KEY` 미설정 + `requests`/`olefile`/`pdfplumber` 미설치.
   `build_dashboard.py`만 로컬 실행 가능. (미결 H-23)
4. 인수인계 문서의 **H-1(제안서 착수 버튼 미구현)은 오판정**이다. 커밋 `41341e0`으로 구현·배포돼 있다.
   판정 전에 `git log origin/main`으로 원격 HEAD를 먼저 확인할 것.
5. **축제·행사 필터는 단어 하나로 자르지 말 것.** `OPS_COND × NOT CRAFT_KEEP` 조합 규칙이다.
   `축제`나 `운영 용역`을 NEG에 넣으면 자사 발표 건(2026 야간경관전시 조성·운영 용역)까지 죽는다. (H-24)

## 위치 판단 — 우선순위를 여기서 놓치면 전부 어긋난다

이 도구는 **그 자체로 돈을 버는 게 아니라, 돈 버는 라인(제안서 수주)에 공고를 밀어 넣는 깔때기**다. "계약 전 단계 전용 도구"이며 시그마애드 로드맵 10개 중 2번, 1번(제안서 수주 = 현재 실제 수익원)의 유입구를 자동화하는 위치에 있다.

**"완전 자동 제안서 공장"은 과장이다.** 자동화되는 구간은 공고 수집 · 기준 추출 · 골격 생성까지고, 승부처 원고와 시각화는 사람이 붙는다. 영업할 때 이 선을 넘어 약속하지 않는다.

진행 상태: ①공고 수집 ②RFP 자동추출 ③제안서 초안 골격 = 실증 완료 / ④Figma 자동 시각화 = 표지 1장까지만, **미검증**.

## 보안 — 반드시 지킬 것

- ⚠ 이전 세션에서 **GitHub PAT가 remote origin URL에 embed된 채로 운용됐고, 별도 보관처는 확인되지 않았다.**
  → 토큰 값을 코드·문서·커밋에 남기지 않는다. 인증은 `gh auth login` 또는 credential helper로 처리한다.
  → remote URL에 토큰이 박혀 있으면 즉시 제거하고 대표께 보고한다.
- 원본 문서에 깨진 문자 표기가 있다(`[ð 서류 N]`, `ð¢적극` 등). 원문자를 확정할 수 없어 `[확인 필요]`로 남아 있다. 임의로 추정해 복원하지 않는다.

## 규칙

- 인물 호칭은 **남실장님**으로 고정.
- 작업이 끝나면 본부 `03_시스템_도구/나라장터_과업서칭/03_진행기록.md`를 갱신하고, 새 미결은 `04_미결.md`에 적는다.

## 통합관리 규약

이 세션은 **이 저장소만** 본다. 통합관리는 본부(HQ) SIGMA 세션이 한다.
보드: `03_시스템_도구/00_개발통합관리.md` — **세션 시작 시 읽는다.**

- 작업 시작: `git pull --rebase`
- 작업 종료: **반드시 커밋·푸시.** 미커밋 상태로 세션을 끝내지 않는다.
- 종료 시 본부 `03_진행기록.md` 갱신 + 새 미결은 `04_미결.md`. 빠뜨리면 통합관리자가 이 작업을 못 본다.
- 프로젝트를 가로지르는 판단(우선순위·가격·상품화·다른 건과의 충돌)은 **여기서 결정하지 않는다.** 보드 §3에 올리고 본부에 넘긴다.
- 다른 저장소(`gijang-map`, `soetae-analysis`)를 건드리지 않는다.

### 🔴 이 저장소는 자동 갱신 봇과 공유한다 — 미커밋이 곧 충돌이다

`narajangteo-bot`이 **원격 클라우드 스케줄**(트리거 `trig_01Ky6Di7tahAcGgtYuuwx6Wq`)로 **매일 10시 KST**에 커밋한다. 이 맥의 전원 상태와 무관하며 7일 연속 실적이 있다.

봇이 매 커밋마다 건드리는 파일: `g2b_dashboard.html` · `index.html` · `attachments.json` · `specs.json` · `store.json` · `g2b_result.csv` · `__pycache__/*.pyc`

→ **이 파일들을 수정했으면 그날 안에 커밋·푸시한다.** 남겨두면 다음 `pull`에서 충돌한다.

### 이 저장소에 걸린 교차 병목 (본부 보드 §3)

- **가격 체계 충돌** — 월 구독(22,000/300,000) vs 건당 고정가. 지도엔진(라움 구독안)과 함께 걸려 있다. Andy 결정
- **자사 실적 데이터 입력** — 시스템으로 대신할 수 없다. Andy 또는 남실장님이 직접

### 기술 부채

- `__pycache__/*.pyc`가 저장소에 커밋되고 있다 → `.gitignore` 필요
- 봇은 **python 3.11**, 이 맥은 **3.9**. 로컬 빌드 산출물이 미묘하게 달라질 수 있다
